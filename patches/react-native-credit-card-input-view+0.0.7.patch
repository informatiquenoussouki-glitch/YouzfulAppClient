diff --git a/node_modules/react-native-credit-card-input-view/src/CardView/CardView.js b/node_modules/react-native-credit-card-input-view/src/CardView/CardView.js
index aeaf64b..be94d73 100644
--- a/node_modules/react-native-credit-card-input-view/src/CardView/CardView.js
+++ b/node_modules/react-native-credit-card-input-view/src/CardView/CardView.js
@@ -1,6 +1,6 @@
 import React from 'react';
 import PropTypes from 'prop-types';
-import { View, ImageBackground, Image, Text, Platform } from 'react-native';
+import { View, Image, Text, Platform } from 'react-native';
 import defaultIcons from '../Icons';
 import FlipCard from 'react-native-flip-card-plus';
 import s from './styles';
@@ -111,9 +111,8 @@ const CardView = (props) => {
         longPressable={true}
         onLongPressed={onLongPressfunc}
         flip={shouldFlip()}>
-        <ImageBackground
-          style={[BASE_SIZE, s.cardFace, transform]}
-          source={imageFront}>
+        <View
+          style={[BASE_SIZE, s.cardFace, transform, { backgroundColor: '#1a1f71', borderRadius: 10, overflow: 'hidden' }]}>
           <Image
             resizeMode={'contain'}
             style={[s.icon]}
@@ -177,10 +176,9 @@ const CardView = (props) => {
               {!cvc ? placeholder.cvc : cvc}
             </Text>
           )}
-        </ImageBackground>
-        <ImageBackground
-          style={[BASE_SIZE, s.cardFace, transform]}
-          source={imageBack}>
+        </View>
+        <View
+          style={[BASE_SIZE, s.cardFace, transform, { backgroundColor: '#2c2c5e', borderRadius: 10, overflow: 'hidden' }]}>
           <Text
             style={[
               s.baseText,
@@ -190,7 +188,7 @@ const CardView = (props) => {
             ]}>
             {!cvc ? placeholder.cvc : cvc}
           </Text>
-        </ImageBackground>
+        </View>
       </FlipCard>
     </View>
   );
