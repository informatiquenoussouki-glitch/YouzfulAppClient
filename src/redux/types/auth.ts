export interface AUTH {
    loggedIn: boolean,
    defaultColor: String,
    isLoading: boolean,
    user: Object,
    token: String,
}
export interface ARRAY {
    id: String,
    code: String,
    name: String,
}